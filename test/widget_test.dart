import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Project setup', () {
    test('placeholder test to keep CI green', () {
      // Real tests will be added as features are built.
      expect(1 + 1, 2);
    });
  });
}